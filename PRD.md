# GeoguessrEX 产品需求文档 (PRD)

> 版本：v0.2.0 ｜ 日期：2026-07-08 ｜ 状态：草稿

---

## 1. 项目背景

### 1.1 产品定位

GeoguessrEX 是一款**以图寻游戏为载体的社交软件**。用户在街景或图片中探索、判断地理位置，在地图上标记猜测点，系统根据距离计算得分。在现有 MVP 单人玩法基础上，本次迭代加入用户系统、社交功能、题库共建与管理后台，使其成为具备 UGC 生态和社交连接的平台。

### 1.2 现有基础（v0.1.0 已实现）

| 模块 | 能力 | 技术实现 |
|---|---|---|
| 单人图寻 | 读取题库 → 展示街景 → 高德地图选点 → 提交 → 计算距离与分数 | React + Express |
| 街景展示 | Google Maps Embed API 加载 360° Street View | iframe 嵌入 |
| 猜点地图 | 高德 AMap JSAPI，支持点击放置标记 | 高德 JSAPI 2.0 |
| 结果地图 | 双标记展示猜测点与答案点 | 高德 JSAPI 2.0 |
| 坐标转换 | 前端 WGS-84 ↔ GCJ-02 互转，后端统一 WGS-84 | coordTransform.js |
| 评分算法 | Haversine 距离公式 + 指数衰减（满分 5000，衰减常数 2000km） | haversine.js + scoring.js |
| 题库 | 本地 JSON 文件，3 道示例题目 | questions.json |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|---|---|---|
| 前端 | Vite + React 18 | 用户端 Web 应用 |
| 地图 | 高德 AMap JSAPI + Google Maps Embed API | 猜点/结果 + 街景 |
| 后端 | Express (Node.js) | RESTful API |
| 数据 | 本地 JSON → 待迁移至数据库 | 见 §5.1 |
| 包管理 | pnpm workspace (monorepo) | `apps/web` + `apps/api` |

---

## 2. 用户角色

| 角色 | 权限范围 | 特征 |
|---|---|---|
| **游客** | 每日 ≤3 局单人练习、浏览社区内容 | 无需注册，数据不持久 |
| **注册用户** | 全部功能：挑战、上传题目、加好友、发动态 | 核心用户群 |
| **审核员** | 题库审核权限 + 注册用户全部权限 | 可由管理员任命 |
| **管理员** | 管理后台全部权限：题库、用户、系统配置 | 最高权限 |

---

## 3. 功能需求

### 3.1 用户系统

#### 3.1.1 注册

| 项 | 说明 |
|---|---|
| 注册方式 | 邮箱 + 密码 |
| 必填字段 | 邮箱、密码、昵称 |
| 密码规则 | ≥ 8 位，必须包含字母和数字 |
| 选填字段 | 头像 |
| 邮箱验证 | 发送 6 位数字验证码，有效期内完成校验 |
| 注册后行为 | 自动登录，跳转至首页 |

#### 3.1.2 登录

| 项 | 说明 |
|---|---|
| 登录方式 | 邮箱 + 密码 |
| 记住我 | 勾选后 7 天内免登录（refresh token） |
| 安全策略 | 密码错误 ≥5 次 → 锁定账号 15 分钟 |
| 忘记密码 | 输入邮箱 → 发送重置链接 → 设置新密码 |

#### 3.1.3 个人主页

| 区域 | 内容 |
|---|---|
| 头部 | 头像、昵称、注册时间、个人简介 |
| 数据面板 | 总游戏局数、平均分、最佳成绩、上传题目数 |
| 动态列表 | 用户最近发布的社区动态（时间倒序） |
| 操作 | 编辑个人信息入口 |

#### 3.1.4 游客模式

| 项 | 说明 |
|---|---|
| 可用功能 | 单人练习（每日 ≤ 3 局）、浏览社区 |
| 受限功能 | 加好友、发动态、上传题目、限时挑战 |
| 排行榜 | 游客成绩不纳入任何排行榜 |
| 数据持久化 | 不持久，清除浏览器数据后丢失 |

---

### 3.2 图寻挑战

#### 3.2.1 单人练习

| 项 | 说明 |
|---|---|
| 题目来源 | 从已通过审核的公共题库中随机抽取 |
| 时间限制 | 无 |
| 每局题数 | 默认 5 题，用户可选 3 / 5 / 10 |
| 提交后展示 | 猜测点与答案点双地图标记、距离（km）、单题得分 |
| 结算面板 | 总分、平均距离、最佳单题、历史对比 |
| 成绩记录 | 写入个人统计，可选择分享至社区 |

#### 3.2.2 限时挑战

| 项 | 说明 |
|---|---|
| 题目来源 | 从已通过审核的公共题库中随机抽取 |
| 时间限制 | 每道题倒计时（默认 60s，管理后台可配） |
| 超时处理 | 该题 0 分，自动进入下一题 |
| 每局题数 | 固定 5 题 |
| 每日次数 | 每用户每日 3 次免费机会 |
| 排行榜 | 独立于练习模式的限时挑战排行榜 |
| 成绩记录 | 写入个人统计 |

#### 3.2.3 题目展示方式

| 模式 | 数据来源 | 展示形式 | 适用场景 |
|---|---|---|---|
| **街景模式** | Google Maps 街景 | iframe 嵌入 360° 全景 | 现有功能，覆盖 Google 街景区域 |
| **图片模式** | 用户上传的静态图片 | 图片全屏/大图展示 | 无街景覆盖地区、特色场景 |

---

### 3.3 题库系统

#### 3.3.1 数据模型

**基础信息**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `id` | uuid | ✅ | 题目唯一标识 |
| `type` | `"streetview"` ｜ `"image"` | ✅ | 题目类型 |
| `title` | string | ✅ | 题目标题 |
| `description` | string | ❌ | 出题者内部备注（不对答题者展示） |
| `tags` | string[] | ❌ | 标签，如国家、城市、场景类型 |
| `difficulty` | `"easy"` ｜ `"medium"` ｜ `"hard"` | 自动 | 系统根据全员答题数据自动计算 |
| `createdAt` | timestamp | 自动 | 创建时间 |

**街景坐标** — `type = "streetview"` 时使用

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `streetView.lat` | number | ✅ | 纬度（WGS-84） |
| `streetView.lng` | number | ✅ | 经度（WGS-84） |
| `streetView.heading` | number | ✅ | 视角朝向（0°~360°） |
| `streetView.pitch` | number | ❌ | 俯仰角（-90°~90°，默认 0） |
| `streetView.fov` | number | ❌ | 视野角度（默认 100） |
| `streetView.panoId` | string | ❌ | Google 街景全景 ID |

**图片坐标** — `type = "image"` 时使用

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `imageUrl` | string | ✅ | 图片存储 URL |
| `imageLat` | number | ✅ | 拍摄位置纬度（WGS-84） |
| `imageLng` | number | ✅ | 拍摄位置经度（WGS-84） |

**审核信息**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `authorId` | uuid | ✅ | 上传者用户 ID |
| `status` | `"pending"` ｜ `"approved"` ｜ `"rejected"` | 自动 | 审核状态 |
| `reviewerId` | uuid | ❌ | 审核员用户 ID |
| `reviewNote` | string | ❌ | 审核备注（驳回时必填） |

#### 3.3.2 上传方式一：Google Maps URL 导入

**概述**：用户在创建题目页面粘贴 Google Maps URL，系统自动解析坐标和视角参数并填充表单。

**支持的 URL 格式**

| 格式 | 示例 |
|---|---|
| 街景分享链接 | `https://www.google.com/maps/@...` |
| 地点详情链接 | `https://www.google.com/maps/place/...` |
| Embed 链接 | `https://www.google.com/maps/embed?...` |

**解析提取字段**

| 提取项 | 对应字段 | 说明 |
|---|---|---|
| 经纬度 | `streetView.lat`, `streetView.lng` | 从 URL 参数中解析 |
| 朝向 | `streetView.heading` | 如有 |
| 俯仰角 | `streetView.pitch` | 如有 |
| 视野 | `streetView.fov` | 如有 |
| 全景 ID | `streetView.panoId` | 如有 |

**流程**：粘贴 URL → 系统解析并自动填充坐标/视角 → 用户补充标题和标签 → 提交审核。

#### 3.3.3 上传方式二：手动录入

**概述**：用户手动输入经纬度并上传图片，适用于非街景场景。

**输入字段**

| 项 | 要求 |
|---|---|
| 经纬度 | 手动输入，WGS-84 十进制格式（如 `39.9042, 116.4074`） |
| 图片 | JPG / PNG / WebP，≤ 10MB，建议 ≥ 1920×1080 |
| 标题 | 必填 |
| 标签 | 选填 |

**图片规则**

| 规则 | 说明 |
|---|---|
| 数量 | 同一地点可上传多张（不同角度），首张作为主图 |
| 存储 | 服务器本地 → 生产环境迁移至对象存储 |
| 适用场景 | 无 Google 街景覆盖的偏远地区、室内场景、特色景观 |

#### 3.3.4 题目审核流程

**Step 1 — 提交**：用户上传题目，状态自动设为 `pending`，题目进入审核队列。

**Step 2 — 分配**：审核员在管理后台查看待审列表，认领题目进行查验。

**Step 3 — 查验**：审核员核对坐标是否匹配、图片是否清晰、内容是否合规。

**Step 4 — 判定**：
- **通过** → 状态变为 `approved`，题目进入公共题库，全员可用。
- **驳回** → 状态变为 `rejected`，审核员在 `reviewNote` 中填写驳回原因，系统向上传者推送通知。

**Step 5 — 重提**（仅驳回后）：上传者在「我的题库」中查看驳回原因，修改题目后重新提交，状态重置为 `pending`，回到 Step 1。

**状态一览**

| 状态 | 含义 | 题目可见范围 |
|---|---|---|
| `pending` | 待审核 | 仅上传者和管理后台 |
| `approved` | 已通过 | 全员可见，可被抽题 |
| `rejected` | 已驳回 | 仅上传者可见 |

**审核标准**

| 检查项 | 判定标准 |
|---|---|
| 坐标匹配 | 坐标是否与图片/街景中展示的实际地理位置一致 |
| 坐标精度 | 坐标定位不能过于模糊，误差应在合理范围内 |
| 图片质量 | 清晰可辨，无过度模糊、大面积遮挡或水印 |
| 内容合规 | 无违规、敏感、色情、暴力等不适宜内容 |

**驳回后续**

| 步骤 | 说明 |
|---|---|
| 1. 填写原因 | 审核员必须在 `reviewNote` 中说明驳回理由 |
| 2. 通知用户 | 系统向上传者推送站内通知 |
| 3. 查看反馈 | 用户在「我的题库」中查看驳回原因 |
| 4. 修改重提 | 用户修改题目信息后重新提交，状态重置为 `pending` |

#### 3.3.5 我的题库

| 功能 | 说明 |
|---|---|
| 题目列表 | 查看自己上传的全部题目，按审核状态筛选（全部 / 待审 / 已通过 / 已驳回） |
| 编辑 | 对「已驳回」题目进行修改后重新提交 |
| 删除 | 删除自己的题目（已通过的题目删除后从公共题库移除） |
| 状态追踪 | 每条题目显示当前审核状态和审核备注 |

---

### 3.4 社交功能

#### 3.4.1 好友系统

**添加好友**

| 步骤 | 说明 |
|---|---|
| 1. 搜索 | 通过昵称或用户 ID 搜索目标用户 |
| 2. 申请 | 点击「添加好友」发送申请 |
| 3. 通知 | 对方收到站内通知 |
| 4. 处理 | 对方选择同意或拒绝 |
| 5. 结果 | 同意后双方互为好友 |

**好友列表**

| 展示字段 | 说明 |
|---|---|
| 头像、昵称 | 基础身份信息 |
| 最近活跃时间 | 如「3 分钟前在线」 |
| 统计数据摘要 | 总局数、平均分 |

**预留**：好友对战功能（v2 迭代）。

#### 3.4.2 社区动态

**发布**

| 项 | 说明 |
|---|---|
| 触发方式 | 完成一局游戏后点击「分享战绩」 |
| 分享内容 | 战绩卡片（题目截图、得分、距离、排名百分比）+ 文字感言 |
| 发布后 | 动态出现在全站动态流中 |

**动态流**

| 项 | 说明 |
|---|---|
| 排序 | 按发布时间倒序 |
| 内容 | 用户头像/昵称、战绩卡片、文字、发布时间 |
| 点击用户 | 跳转至该用户个人主页 |

**互动**

| 操作 | 说明 |
|---|---|
| 点赞 | 对动态点赞，显示点赞数 |
| 评论 | 文字评论，评论者之间可查看彼此回复 |

#### 3.4.3 排行榜

**榜单类型**

| 榜单 | 说明 | 重置周期 |
|---|---|---|
| 总积分榜 | 累计得分排名 | 不重置 |
| 周榜 | 本周累计得分排名 | 每周一 00:00 |
| 日榜 | 今日累计得分排名 | 每日 00:00 |
| 限时挑战榜 | 限时挑战模式最佳单局得分排名 | 独立榜单，不重置 |
| 好友榜 | 仅展示好友间的排名 | 跟随全局榜单 |

**展示字段**

| 字段 | 说明 |
|---|---|
| 排名 | 当前名次 |
| 头像 | 用户头像 |
| 昵称 | 用户昵称 |
| 分数 | 对应榜单的得分 |
| 局数 | 完成总局数 |
| 平均距离 | 所有题目的平均误差距离 |

---

### 3.5 Web 管理后台

> 独立前端应用（`apps/admin`），仅管理员和审核员可访问。

#### 3.5.1 题库管理

**审核队列**

| 功能 | 说明 |
|---|---|
| 待审列表 | 按提交时间排序，展示题目摘要 |
| 预览 | 查看街景 iframe 或图片，在地图上确认坐标位置 |
| 审核操作 | 一键通过 / 驳回 + 填写`reviewNote` |
| 批量操作 | 勾选多道题目，批量通过或驳回 |

**题库列表**

| 功能 | 说明 |
|---|---|
| 筛选 | 按状态、类型、上传者、标签筛选 |
| 排序 | 按创建时间、难度、答题次数排序 |
| 编辑 | 修改题目任意字段 |
| 下架/删除 | 从公共题库移除或永久删除 |

#### 3.5.2 用户管理

| 功能 | 说明 |
|---|---|
| 用户列表 | 搜索、按角色筛选、按注册时间排序 |
| 用户详情 | 查看个人信息、统计数据、最近活动 |
| 封禁/解封 | 封禁后用户无法登录和使用任何功能 |
| 角色设置 | 分配角色：普通用户 / 审核员 / 管理员 |

#### 3.5.3 数据统计

| 模块 | 指标 |
|---|---|
| 平台概览 | 总注册用户数、总题目数（按状态分布）、今日游戏局数 |
| 题库统计 | 待审/通过/驳回题目数量、审核通过率、人均上传数 |
| 用户趋势 | 日新增注册、日活跃用户数 |
| 题目排行 | 被答题次数最多的题目、平均得分最高的题目 |

#### 3.5.4 系统配置

| 配置项 | 说明 | 默认值 |
|---|---|---|
| 限时挑战倒计时 | 每道题的作答时限 | 60 秒 |
| 游客每日局数 | 游客每天可免费练习的局数 | 3 局 |
| 限时挑战每日次数 | 每用户每日免费挑战次数 | 3 次 |
| 评分满分 | `scoreFromDistance` 的 maxScore | 5000 |
| 评分衰减常数 | `scoreFromDistance` 的 decay | 2000km |
| 练习默认题数 | 单人练习每局默认题目数 | 5 题 |

---

## 4. 非功能需求

### 4.1 性能

| 指标 | 目标值 |
|---|---|
| API 响应时间（P95） | < 500ms |
| 图片首屏加载 | < 2s（CDN 加速后） |
| 并发在线用户 | ≥ 500 |
| 图片上传响应 | < 3s |

### 4.2 安全

| 项 | 措施 |
|---|---|
| 密码存储 | bcrypt 加盐哈希 |
| 身份认证 | JWT（access token 2h 有效期 + refresh token 7 天） |
| 文件上传 | 校验文件扩展名 + MIME 类型 + 大小上限，服务端二次校验 |
| Web 安全 | 防 XSS（输出转义）、防 CSRF（SameSite Cookie + Token）、参数化查询防 SQL 注入 |
| 接口限流 | 注册/登录接口：每 IP 每分钟 ≤ 10 次；普通 API：每用户每分钟 ≤ 60 次 |

### 4.3 可维护性

| 项 | 措施 |
|---|---|
| API 规范 | RESTful，用户端路径统一 `/api/` 前缀，管理后台路径统一 `/api/admin/` 前缀 |
| 数据库变更 | migration 脚本管理，支持版本回滚 |
| 测试 | 核心业务逻辑（评分、坐标转换、审核状态机）编写单元测试 |
| 日志 | 请求日志 + 错误日志 + 审核操作日志 |

### 4.4 兼容性

| 端 | 要求 |
|---|---|
| 浏览器 | Chrome / Firefox / Edge 最新两个大版本 |
| 分辨率 | 桌面端，最低支持 1280×720 |

---

## 5. 技术建议

### 5.1 数据库

| 阶段 | 方案 | 说明 |
|---|---|---|
| 开发 | SQLite | 零配置，单文件，适合快速迭代 |
| 生产 | PostgreSQL + PostGIS | 地理空间查询支持（如「附近题目」「区域筛选」） |

### 5.2 文件存储

| 阶段 | 方案 |
|---|---|
| 开发 | 本地文件系统（`apps/api/uploads/`） |
| 生产 | 对象存储（阿里云 OSS / AWS S3）+ CDN 加速 |

### 5.3 目录结构

| 目录 | 说明 | 状态 |
|---|---|---|
| `apps/api/` | 后端 API | 现有 |
| `apps/web/` | 用户端前端 | 现有 |
| `apps/admin/` | 管理后台前端 | 新增 |

### 5.4 后端模块划分

**路由层 — `apps/api/src/routes/`**

| 文件 | 职责 | 主要接口 |
|---|---|---|
| `auth.js` | 注册、登录、登出、密码重置 | `POST /api/auth/register` `POST /api/auth/login` `POST /api/auth/logout` `POST /api/auth/reset-password` |
| `users.js` | 用户信息查询与修改 | `GET /api/users/:id` `PUT /api/users/:id` |
| `questions.js` | 题库查询 | `GET /api/questions` `GET /api/questions/:id` |
| `upload.js` | 题目上传（URL 解析 + 图片上传） | `POST /api/questions` |
| `game.js` | 游戏回合提交与会话管理 | `POST /api/submit` `GET /api/game/session` |
| `social.js` | 好友、动态、评论、点赞 | 好友 CRUD、动态发布/流、评论、点赞 |
| `leaderboard.js` | 排行榜查询 | `GET /api/leaderboard?type=global\|weekly\|daily\|friends` |
| `admin/questions.js` | 管理端题库操作 | 审核通过/驳回、编辑、删除 |
| `admin/users.js` | 管理端用户操作 | 列表查询、封禁/解封、角色分配 |
| `admin/stats.js` | 管理端数据统计 | 平台概览、用户趋势、题目排行 |
| `admin/config.js` | 管理端系统配置 | 读取/修改系统参数 |

**服务层 — `apps/api/src/services/`**

| 文件 | 职责 |
|---|---|
| `authService.js` | 注册逻辑、密码哈希、Token 签发与校验 |
| `userService.js` | 用户 CRUD、个人统计聚合 |
| `uploadService.js` | URL 解析、图片存储、题目入库 |
| `feedService.js` | 动态流生成、点赞/评论逻辑 |
| `leaderboardService.js` | 榜单计算与缓存 |

**中间件 — `apps/api/src/middleware/`**

| 文件 | 职责 |
|---|---|
| `auth.js` | JWT 鉴权，校验 access token |
| `admin.js` | 管理员/审核员权限校验 |
| `rateLimiter.js` | 接口请求频率限制 |
| `upload.js` | 文件上传类型与大小校验 |

**数据层 — `apps/api/src/models/`**

| 内容 | 说明 |
|---|---|
| ORM 模型定义 | 用户、题目、动态、评论、好友关系等实体映射 |

---

## 6. 版本规划

| 版本 | 主题 | 交付内容 |
|---|---|---|
| **v0.2** | 用户系统 + 题库升级 | 邮箱注册/登录、JWT 鉴权、Google Maps URL 导入、图片手动录入、审核流程、数据库迁移 |
| **v0.3** | 社交核心 | 好友系统、个人主页、社区动态（分享+点赞+评论）、排行榜（全局/周榜/日榜/好友榜） |
| **v0.4** | 限时挑战 + 管理后台 | 限时挑战模式（倒计时+独立排行）、管理后台（题库审核/用户管理/数据统计/系统配置） |
| **v0.5** | 体验优化 | CDN + 缓存、游客模式、性能调优 |
| **v1.0** | 正式版 | 全面测试与缺陷修复、生产环境部署、运营数据看板 |

---

## 7. 开放问题

| 序号 | 问题 | 影响范围 |
|---|---|---|
| 1 | **头像存储**：Gravatar（基于邮箱）还是自主上传？ | 注册流程、文件存储 |
| 2 | **题目去重**：同一坐标附近（如 50m 内）是否允许多道题？ | 题库设计、审核标准 |
| 3 | **敏感位置**：是否过滤军事区、私人住宅等敏感坐标？ | 审核标准、合规 |
| 4 | **国际化**：初期仅中文，是否预留多语言？ | 前端架构、数据库设计 |
| 5 | **Google 依赖**：国内访问 Google 街景受限，备选方案？ | 街景展示、题库来源 |

